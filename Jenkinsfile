pipeline {
    agent any 
    stages {
        stage('Clone the repo') {
            steps {
                git clone 'https://github.com/CamiloMoreno09/paginaweb.git'
            }
        }
    }
}
