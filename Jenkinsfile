pipeline {
    agent any 
    stages {
        stage('Clone the repo') {
            steps {
                but 'git clone https://github.com/CamiloMoreno09/paginaweb.git'
            }
        }
    }
}
